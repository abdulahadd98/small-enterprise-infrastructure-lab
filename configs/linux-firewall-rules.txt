# LINUX01 UFW rules used in the lab
sudo ufw allow 22/tcp
sudo ufw allow 80/tcp
sudo ufw enable
sudo ufw status verbose

# Intended policy:
# - Default incoming: deny
# - Default outgoing: allow
# - SSH 22/tcp: allow
# - HTTP 80/tcp: allow
